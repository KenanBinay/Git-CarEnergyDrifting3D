using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Score_Controller : MonoBehaviour
{
    public static float _scoreMain;

    void Start()
    {
        
    }

    void Update()
    {
     /*   if (CarController.gameEnd == false && LevelEndController.lvlEndEnter == false)
        {
            _scoreMain += 2 * Time.deltaTime;
        }
        else if (CarController.gameEnd || LevelEndController.lvlEndEnter)
        {
            Debug.Log("FINAL SCORE : " + _scoreMain);
        }
     */
    }

}
